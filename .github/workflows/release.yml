name: Build and Release
on:
  push:
    tags:
      - 'v*'
permissions:
  id-token: write
  contents: write
  attestations: write
jobs:
  release-ricardian-registry:
    uses: stellar-expert/soroban-build-workflow/.github/workflows/release.yml@main
    with:
      release_name: ${{ github.ref_name }}
      release_description: 'Ricardian Registry contract release'
      package: 'ricardian-registry'
    secrets:
      release_token: ${{ secrets.GITHUB_TOKEN }}
